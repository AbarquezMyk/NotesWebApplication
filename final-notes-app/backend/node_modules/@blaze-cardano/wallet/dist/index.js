//#region rolldown:runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));

//#endregion
let __blaze_cardano_core = require("@blaze-cardano/core");
__blaze_cardano_core = __toESM(__blaze_cardano_core);
let __blaze_cardano_tx_value = require("@blaze-cardano/tx/value");
__blaze_cardano_tx_value = __toESM(__blaze_cardano_tx_value);

//#region src/types.ts
/**
* Abstract class for Wallet.
*/
var Wallet = class {};

//#endregion
//#region src/wallets.ts
/**
* The details of the wallets.
*/
const WalletDetails = [{
	namespace: "nami",
	name: "Nami Wallet",
	websiteUrl: "https://www.namiwallet.io/"
}];

//#endregion
//#region src/web.ts
/**
* Wallet class that interacts with the WalletInterface.
*/
var WebWallet = class {
	/**
	* Constructs a new instance of the WebWallet class.
	* @param {CIP30Interface} webWallet - The CIP30Interface to be used.
	*/
	constructor(webWallet) {
		this.webWallet = webWallet;
	}
	/**
	* Retrieves the network ID of the currently connected account.
	* @returns {Promise<NetworkId>} - The network ID of the currently connected account.
	*/
	async getNetworkId() {
		switch (await this.webWallet.getNetworkId()) {
			case 0: return 0;
			case 1: return 1;
			default: throw new Error("Could not resolve CIP30 network id.");
		}
	}
	/**
	* Retrieves the UTxO(s) controlled by the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The UTXO(s) controlled by the wallet.
	*/
	async getUnspentOutputs() {
		return (await this.webWallet.getUtxos() ?? []).map((utxo) => __blaze_cardano_core.TransactionUnspentOutput.fromCbor((0, __blaze_cardano_core.HexBlob)(utxo)));
	}
	/**
	* Retrieves the total available balance of the wallet, encoded in CBOR.
	* @returns {Promise<Value>} - The balance of the wallet.
	*/
	async getBalance() {
		const balance = await this.webWallet.getBalance();
		return __blaze_cardano_core.Value.fromCbor((0, __blaze_cardano_core.HexBlob)(balance));
	}
	/**
	* Retrieves all used addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The used addresses controlled by the wallet.
	*/
	async getUsedAddresses() {
		return (await this.webWallet.getUsedAddresses()).map((addy) => {
			const parsedAddy = __blaze_cardano_core.Address.fromString(addy);
			if (!parsedAddy) throw new Error("getUsedAddresses: Wallet API returned an invalid used address.");
			return parsedAddy;
		});
	}
	/**
	* Retrieves all unused addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The unused addresses controlled by the wallet.
	*/
	async getUnusedAddresses() {
		return (await this.webWallet.getUnusedAddresses()).map((addy) => {
			const parsedAddy = __blaze_cardano_core.Address.fromString(addy);
			if (!parsedAddy) throw new Error("getUnusedAddresses: Wallet API returned an invalid unused address.");
			return parsedAddy;
		});
	}
	/**
	* Retrieves an address owned by the wallet which should be used to return transaction change.
	* @returns {Promise<Address>} - The change address.
	*/
	async getChangeAddress() {
		const addy = await this.webWallet.getChangeAddress();
		const parsedAddy = __blaze_cardano_core.Address.fromString(addy);
		if (!parsedAddy) throw new Error("getChangeAddress: Wallet API returned an invalid change address.");
		return parsedAddy;
	}
	/**
	* Retrieves the reward addresses controlled by the wallet.
	* @returns {Promise<RewardAddress[]>} - The reward addresses controlled by the wallet.
	*/
	async getRewardAddresses() {
		return (await this.webWallet.getRewardAddresses()).map((addy) => {
			const parsedAddy = __blaze_cardano_core.Address.fromString(addy);
			if (!parsedAddy) throw new Error("getRewardAddresses: Wallet API returned an invalid address.");
			const parsedRewardAddy = __blaze_cardano_core.RewardAddress.fromAddress(parsedAddy);
			if (!parsedRewardAddy) throw new Error("getRewardAddresses: Wallet API returned an invalid reward address.");
			return parsedRewardAddy;
		});
	}
	/**
	* Requests a transaction signature from the wallet.
	* @param {string} tx - The transaction to sign.
	* @param {boolean} partialSign - Whether to partially sign the transaction.
	* @returns {Promise<TransactionWitnessSet>} - The signed transaction.
	*/
	async signTransaction(tx, partialSign) {
		const witnessSet = await this.webWallet.signTx(tx.toCbor(), partialSign);
		return __blaze_cardano_core.TransactionWitnessSet.fromCbor((0, __blaze_cardano_core.HexBlob)(witnessSet));
	}
	/**
	* Requests signed data from the wallet.
	* @param {Address} address - The address to sign the data with.
	* @param {string} payload - The data to sign.
	* @returns {Promise<CIP30DataSignature>} - The signed data.
	*/
	async signData(address, payload) {
		const { signature, key } = await this.webWallet.signData(address.toBech32(), payload);
		return {
			key: (0, __blaze_cardano_core.HexBlob)(key),
			signature: (0, __blaze_cardano_core.HexBlob)(signature)
		};
	}
	/**
	* Submits a transaction through the wallet.
	* @param {Transaction} tx - The transaction to submit.
	* @returns {Promise<TransactionId>} - The ID of the submitted transaction.
	*/
	async postTransaction(tx) {
		const transactionId = await this.webWallet.submitTx(tx.toCbor());
		return __blaze_cardano_core.TransactionId.fromHexBlob((0, __blaze_cardano_core.HexBlob)(transactionId));
	}
	/**
	* Retrieves the collateral UTxO(s) for the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The collateral for the wallet.
	*/
	async getCollateral() {
		return (await this.webWallet.getCollateral() ?? []).map((utxo) => __blaze_cardano_core.TransactionUnspentOutput.fromCbor((0, __blaze_cardano_core.HexBlob)(utxo)));
	}
};

//#endregion
//#region src/utils.ts
async function signData(addressHex, payload, privateKey) {
	const { Label, CBORValue, COSESign1Builder, Headers, HeaderMap, ProtectedHeaderMap, Int, BigNum, AlgorithmId, COSEKey, KeyType } = await (typeof window === "undefined" ? import("@emurgo/cardano-message-signing-nodejs") : import("@emurgo/cardano-message-signing-browser"));
	const protectedHeaders = HeaderMap.new();
	protectedHeaders.set_algorithm_id(Label.from_algorithm_id(AlgorithmId.EdDSA));
	protectedHeaders.set_header(Label.new_text("address"), CBORValue.new_bytes((0, __blaze_cardano_core.fromHex)(addressHex)));
	const protectedSerialized = ProtectedHeaderMap.new(protectedHeaders);
	const unprotectedHeaders = HeaderMap.new();
	const headers = Headers.new(protectedSerialized, unprotectedHeaders);
	const builder = COSESign1Builder.new(headers, (0, __blaze_cardano_core.fromHex)(payload), false);
	const toSign = builder.make_data_to_sign().to_bytes();
	const publicKey = await privateKey.toPublic();
	const signedSigStruc = await privateKey.sign((0, __blaze_cardano_core.HexBlob)((0, __blaze_cardano_core.toHex)(toSign)));
	const coseSign1 = builder.build(signedSigStruc.bytes());
	const key = COSEKey.new(Label.from_key_type(KeyType.OKP));
	key.set_algorithm_id(Label.from_algorithm_id(AlgorithmId.EdDSA));
	key.set_header(Label.new_int(Int.new_negative(BigNum.from_str("1"))), CBORValue.new_int(Int.new_i32(6)));
	key.set_header(Label.new_int(Int.new_negative(BigNum.from_str("2"))), CBORValue.new_bytes(publicKey.bytes()));
	return {
		signature: (0, __blaze_cardano_core.HexBlob)((0, __blaze_cardano_core.toHex)(coseSign1.to_bytes())),
		key: (0, __blaze_cardano_core.HexBlob)((0, __blaze_cardano_core.toHex)(key.to_bytes()))
	};
}

//#endregion
//#region src/hot.ts
/**
* Wallet class that interacts with the HotWallet.
*/
var HotWallet = class HotWallet {
	/**
	* Constructs a new instance of the HotWallet class.
	* @param {Address} address - the wallets's address
	* @param {RewardAddress} rewardAddress - the wallet's reward address if there is any
	* @param {Bip32PrivateKey} signingKey - The signing key of the derived account's of wallet.
	* @param {Bip32PublicKey} publicKey - The public key of the derived account's of the wallet.
	* @param {Provider} provider - The provider of the wallet.
	*/
	constructor(address, rewardAddress, signingKey, publicKey, provider, stakePaymentKey) {
		this.address = address;
		this.rewardAddress = rewardAddress;
		this.networkId = this.address.getNetworkId();
		this.signingKey = signingKey;
		this.publicKey = publicKey;
		this.provider = provider;
		this.stakeSigningKey = stakePaymentKey;
	}
	static {
		this.harden = (num) => 2147483648 + num;
	}
	static async generateAccountAddressFromMasterkey(masterkey, networkId = __blaze_cardano_core.NetworkId.Testnet, addressType = __blaze_cardano_core.AddressType.BasePaymentKeyStakeKey) {
		if (addressType !== __blaze_cardano_core.AddressType.BasePaymentKeyStakeKey && addressType !== __blaze_cardano_core.AddressType.EnterpriseKey) throw new Error("Hot wallets only support the BasePaymentKeyStakeKey and EnterpriseKey adresses!");
		const accountKey = await masterkey.derive([
			this.harden(1852),
			this.harden(1815),
			this.harden(0)
		]);
		const paymentKey = await accountKey.derive([0, 0]);
		const stakePaymentKey = await accountKey.derive([2, 0]);
		return {
			address: new __blaze_cardano_core.Address({
				type: addressType,
				networkId,
				paymentPart: {
					type: __blaze_cardano_core.CredentialType.KeyHash,
					hash: __blaze_cardano_core.Hash28ByteBase16.fromEd25519KeyHashHex((await (await paymentKey.toPublic()).toRawKey().hash()).hex())
				},
				delegationPart: addressType === __blaze_cardano_core.AddressType.EnterpriseKey ? void 0 : {
					type: __blaze_cardano_core.CredentialType.KeyHash,
					hash: __blaze_cardano_core.Hash28ByteBase16.fromEd25519KeyHashHex((await (await (await accountKey.derive([2, 0])).toPublic()).toRawKey().hash()).hex())
				}
			}),
			paymentKey,
			stakePaymentKey,
			publicKey: await paymentKey.toPublic()
		};
	}
	static async fromMasterkey(masterkey, provider, networkId = __blaze_cardano_core.NetworkId.Testnet, addressType = __blaze_cardano_core.AddressType.BasePaymentKeyStakeKey) {
		const rootKey = __blaze_cardano_core.Bip32PrivateKey.fromHex(masterkey);
		const { address, paymentKey, stakePaymentKey, publicKey } = await this.generateAccountAddressFromMasterkey(rootKey, networkId, addressType);
		return new HotWallet(address, address.asReward(), paymentKey, publicKey, provider, stakePaymentKey);
	}
	/**
	* Retrieves the network ID of the currently connected account.
	* @returns {Promise<NetworkId>} - The network ID of the currently connected account.
	*/
	async getNetworkId() {
		return this.networkId;
	}
	/**
	* Retrieves the UTxO(s) controlled by the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The UTXO(s) controlled by the wallet.
	*/
	async getUnspentOutputs() {
		return this.provider.getUnspentOutputs(this.address);
	}
	/**
	* Retrieves the total available balance of the wallet, encoded in CBOR.
	* @returns {Promise<Value>} - The balance of the wallet.
	*/
	async getBalance() {
		let balance = __blaze_cardano_tx_value.zero();
		const utxos = await this.getUnspentOutputs();
		for (const utxo of utxos) balance = __blaze_cardano_tx_value.merge(balance, utxo.output().amount());
		return balance;
	}
	/**
	* Retrieves all used addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The used addresses controlled by the wallet.
	*/
	async getUsedAddresses() {
		return [this.address];
	}
	/**
	* Retrieves all unused addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The unused addresses controlled by the wallet.
	*/
	async getUnusedAddresses() {
		return [];
	}
	/**
	* Retrieves an address owned by the wallet which should be used to return transaction change.
	* @returns {Promise<Address>} - The change address.
	*/
	async getChangeAddress() {
		return this.address;
	}
	/**
	* Retrieves the reward addresses controlled by the wallet.
	* @returns {Promise<RewardAddress[]>} - The reward addresses controlled by the wallet.
	*/
	async getRewardAddresses() {
		return this.rewardAddress ? [this.rewardAddress] : [];
	}
	/**
	* Requests a transaction signature from the wallet.
	* @param {string} tx - The transaction to sign.
	* @param {boolean} partialSign - Whether to partially sign the transaction.
	* @param {boolean} signWithStakeKey - Whether to also sign the transaction with the stake key.
	* @returns {Promise<TransactionWitnessSet>} - The signed transaction.
	*/
	async signTransaction(tx, partialSign = true, signWithStakeKey = false) {
		if (partialSign == false) throw new Error("signTx: Hot wallet only supports partial signing = true");
		const signature = await this.signingKey.toRawKey().sign((0, __blaze_cardano_core.HexBlob)(tx.getId()));
		const tws = new __blaze_cardano_core.TransactionWitnessSet();
		const vkeys = [new __blaze_cardano_core.VkeyWitness(this.publicKey.toRawKey().hex(), signature.hex()).toCore()];
		if (signWithStakeKey) {
			if (!this.stakeSigningKey) throw new Error("signTx: Signing with stake key requested but no stake key is available");
			const stakeSignature = await this.stakeSigningKey.toRawKey().sign((0, __blaze_cardano_core.HexBlob)(tx.getId()));
			const stakeVkw = new __blaze_cardano_core.VkeyWitness((await this.stakeSigningKey.toPublic()).toRawKey().hex(), stakeSignature.hex());
			vkeys.push(stakeVkw.toCore());
		}
		tws.setVkeys(__blaze_cardano_core.CborSet.fromCore(vkeys, __blaze_cardano_core.VkeyWitness.fromCore));
		return tws;
	}
	/**
	* Requests signed data from the wallet.
	* @param {Address} address - The address to sign the data with.
	* @param {string} payload - The data to sign.
	* @returns {Promise<CIP30DataSignature>} - The signed data.
	*/
	async signData(address, payload) {
		const paymentKey = address.getProps().paymentPart;
		const signingPublic = await this.signingKey.toPublic();
		const stakeSigningPublic = await this.stakeSigningKey?.toPublic();
		const signingKeyHash = await signingPublic.toRawKey().hash();
		const stakeSigningKeyHash = await stakeSigningPublic?.toRawKey().hash();
		if (!paymentKey) throw new Error("signData: Address does not have a payment key");
		const signingKeyMap = {};
		if (signingKeyHash) signingKeyMap[signingKeyHash.hex().toString()] = this.signingKey;
		if (stakeSigningKeyHash) signingKeyMap[stakeSigningKeyHash.hex().toString()] = this.stakeSigningKey;
		const signingKey = signingKeyMap[paymentKey.hash.toString()];
		if (!signingKey) throw new Error("signData: Address does not have a signing key");
		return signData(address.toBytes(), payload, signingKey.toRawKey());
	}
	/**
	* Submits a transaction through the wallet.
	* @param {string} tx - The transaction to submit.
	* @returns {Promise<TransactionId>} - The ID of the submitted transaction.
	*/
	async postTransaction(tx) {
		return this.provider.postTransactionToChain(tx);
	}
	/**
	* Retrieves the collateral UTxO(s) for the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The collateral for the wallet.
	*/
	async getCollateral() {
		return [];
	}
};

//#endregion
//#region src/hotsingle.ts
/**
* Wallet class that interacts with the HotSingleWallet.
* This is like HotWallet, but without key derivation.
*/
var HotSingleWallet = class {
	/**
	* Constructs a new instance of the HotSingleWallet class.
	* @param {Ed25519PrivateNormalKeyHex} signingKey - The private signing key of the wallet.
	* @param {NetworkId} networkId - The network ID for the wallet.
	* @param {Provider} provider - The provider for the wallet.
	* @param {Ed25519PrivateNormalKeyHex} stakeSigningKey - An optional private signing key for the delegation part of the wallet. If not provided, the wallet will have an enterprise address.
	*/
	constructor(paymentSigningKey, networkId, provider, stakeSigningKey) {
		this.stakeSigningKey = void 0;
		this.stakePublicKey = void 0;
		this.networkId = networkId;
		this.paymentSigningKey = paymentSigningKey;
		this.paymentPublicKey = __blaze_cardano_core.Ed25519PublicKey.fromHex((0, __blaze_cardano_core.derivePublicKey)(this.paymentSigningKey));
		if (stakeSigningKey) {
			this.stakeSigningKey = stakeSigningKey;
			this.stakePublicKey = __blaze_cardano_core.Ed25519PublicKey.fromHex((0, __blaze_cardano_core.derivePublicKey)(this.stakeSigningKey));
		}
		this.address = new __blaze_cardano_core.Address({
			type: this.stakePublicKey ? __blaze_cardano_core.AddressType.BasePaymentKeyStakeKey : __blaze_cardano_core.AddressType.EnterpriseKey,
			networkId: this.networkId,
			paymentPart: {
				type: __blaze_cardano_core.CredentialType.KeyHash,
				hash: (0, __blaze_cardano_core.blake2b_224)((0, __blaze_cardano_core.HexBlob)(this.paymentPublicKey.hex()))
			},
			delegationPart: this.stakePublicKey ? {
				type: __blaze_cardano_core.CredentialType.KeyHash,
				hash: (0, __blaze_cardano_core.blake2b_224)((0, __blaze_cardano_core.HexBlob)(this.stakePublicKey.hex()))
			} : void 0
		});
		this.provider = provider;
	}
	/**
	* Retrieves the network ID of the currently connected account.
	* @returns {Promise<NetworkId>} - The network ID of the currently connected account.
	*/
	async getNetworkId() {
		return this.networkId;
	}
	/**
	* Retrieves the UTxO(s) controlled by the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The UTXO(s) controlled by the wallet.
	*/
	async getUnspentOutputs() {
		return this.provider.getUnspentOutputs(this.address);
	}
	/**
	* Retrieves the total available balance of the wallet, encoded in CBOR.
	* @returns {Promise<Value>} - The balance of the wallet.
	*/
	async getBalance() {
		return (await this.getUnspentOutputs()).reduce((x, y) => __blaze_cardano_tx_value.merge(x, y.output().amount()), __blaze_cardano_tx_value.zero());
	}
	/**
	* Retrieves all used addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The used addresses controlled by the wallet.
	*/
	async getUsedAddresses() {
		return [this.address];
	}
	/**
	* Retrieves all unused addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The unused addresses controlled by the wallet.
	*/
	async getUnusedAddresses() {
		return [];
	}
	/**
	* Retrieves an address owned by the wallet which should be used to return transaction change.
	* @returns {Promise<Address>} - The change address.
	*/
	async getChangeAddress() {
		return this.address;
	}
	/**
	* Retrieves the reward addresses controlled by the wallet.
	* @returns {Promise<RewardAddress[]>} - The reward addresses controlled by the wallet.
	*/
	async getRewardAddresses() {
		if (this.stakePublicKey) {
			const rewardAddress = new __blaze_cardano_core.Address({
				type: __blaze_cardano_core.AddressType.RewardKey,
				networkId: this.networkId,
				paymentPart: this.address.getProps().delegationPart
			}).asReward();
			return rewardAddress ? [rewardAddress] : [];
		}
		return [];
	}
	/**
	* Requests a transaction signature from the wallet.
	* @param {string} tx - The transaction to sign.
	* @param {boolean} partialSign - Whether to partially sign the transaction.
	* @returns {Promise<TransactionWitnessSet>} - The signed transaction.
	*/
	async signTransaction(tx, partialSign = true) {
		if (partialSign == false) throw new Error("signTx: Hot single wallet only supports partial signing = true");
		const signature = (0, __blaze_cardano_core.signMessage)((0, __blaze_cardano_core.HexBlob)(tx.getId()), this.paymentSigningKey);
		const tws = new __blaze_cardano_core.TransactionWitnessSet();
		const vkw = new __blaze_cardano_core.VkeyWitness(this.paymentPublicKey.hex(), (0, __blaze_cardano_core.Ed25519SignatureHex)(signature));
		tws.setVkeys(__blaze_cardano_core.CborSet.fromCore([vkw.toCore()], __blaze_cardano_core.VkeyWitness.fromCore));
		return tws;
	}
	/**
	* Requests signed data from the wallet.
	* Not supported, will always throw an error.
	* @param {Address} address - The address to sign the data with.
	* @param {string} payload - The data to sign.
	* @returns {Promise<CIP30DataSignature>} - The signed data.
	*/
	async signData(address, payload) {
		const paymentKey = address.getProps().paymentPart;
		const paymentSigningKey = __blaze_cardano_core.Ed25519PrivateKey.fromNormalHex(this.paymentSigningKey);
		const stakeSigningKey = this.stakeSigningKey && __blaze_cardano_core.Ed25519PrivateKey.fromNormalHex(this.stakeSigningKey);
		const signingPublic = await paymentSigningKey.toPublic();
		const stakeSigningPublic = await stakeSigningKey?.toPublic();
		const signingKeyHash = await signingPublic.hash();
		const stakeSigningKeyHash = await stakeSigningPublic?.hash();
		if (!paymentKey) throw new Error("signData: Address does not have a payment key");
		const signingKeyMap = {};
		if (signingKeyHash) signingKeyMap[signingKeyHash.hex().toString()] = paymentSigningKey;
		if (stakeSigningKeyHash) signingKeyMap[stakeSigningKeyHash.hex().toString()] = stakeSigningKey;
		const signingKey = signingKeyMap[paymentKey.hash.toString()];
		if (!signingKey) throw new Error("signData: Address does not have a signing key");
		return signData(address.toBytes(), payload, signingKey);
	}
	/**
	* Submits a transaction through the wallet.
	* @param {string} tx - The transaction to submit.
	* @returns {Promise<TransactionId>} - The ID of the submitted transaction.
	*/
	async postTransaction(tx) {
		return this.provider.postTransactionToChain(tx);
	}
	/**
	* Retrieves the collateral UTxO(s) for the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The collateral for the wallet.
	*/
	async getCollateral() {
		return [];
	}
};

//#endregion
//#region src/cold.ts
/**
* Wallet class that interacts with the ColdWallet.
*/
var ColdWallet = class {
	/**
	* Constructs a new instance of the ColdWallet class.
	* @param {Address} address - The address of the wallet.
	* @param {NetworkId} networkId - The network ID of the wallet.
	* @param {Provider} provider - The provider of the wallet.
	*/
	constructor(address, networkId, provider) {
		this.networkId = networkId;
		this.address = address;
		this.provider = provider;
	}
	/**
	* Retrieves the network ID of the currently connected account.
	* @returns {Promise<NetworkId>} - The network ID of the currently connected account.
	*/
	async getNetworkId() {
		return this.networkId;
	}
	/**
	* Retrieves the UTxO(s) controlled by the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The UTXO(s) controlled by the wallet.
	*/
	async getUnspentOutputs() {
		return this.provider.getUnspentOutputs(this.address);
	}
	/**
	* Retrieves the total available balance of the wallet, encoded in CBOR.
	* @returns {Promise<Value>} - The balance of the wallet.
	*/
	async getBalance() {
		let balance = __blaze_cardano_tx_value.zero();
		const utxos = await this.getUnspentOutputs();
		for (const utxo of utxos) balance = __blaze_cardano_tx_value.merge(balance, utxo.output().amount());
		return balance;
	}
	/**
	* Retrieves all used addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The used addresses controlled by the wallet.
	*/
	async getUsedAddresses() {
		return Promise.resolve([this.address]);
	}
	/**
	* Retrieves all unused addresses controlled by the wallet.
	* @returns {Promise<Address[]>} - The unused addresses controlled by the wallet.
	*/
	async getUnusedAddresses() {
		return [];
	}
	/**
	* Retrieves an address owned by the wallet which should be used to return transaction change.
	* @returns {Promise<Address>} - The change address.
	*/
	async getChangeAddress() {
		return this.address;
	}
	/**
	* Retrieves the reward addresses controlled by the wallet.
	* @returns {Promise<RewardAddress[]>} - The reward addresses controlled by the wallet.
	*/
	async getRewardAddresses() {
		return [];
	}
	/**
	* Requests a transaction signature from the wallet.
	* @param {string} tx - The transaction to sign.
	* @param {boolean} partialSign - Whether to partially sign the transaction.
	* @returns {Promise<TransactionWitnessSet>} - The signed transaction.
	*/
	async signTransaction(_tx, _partialSign = true) {
		throw new Error("ColdWallet signTransaction: Cold wallet cannot sign a transaction!");
	}
	/**
	* Requests signed data from the wallet.
	* @param {Address} address - The address to sign the data with.
	* @param {string} payload - The data to sign.
	* @returns {Promise<CIP30DataSignature>} - The signed data.
	*/
	async signData(_address, _payload) {
		throw new Error("ColdWallet signData: Cold wallet cannot sign data!");
	}
	/**
	* Submits a transaction through the wallet.
	* @param {string} tx - The transaction to submit.
	* @returns {Promise<TransactionId>} - The ID of the submitted transaction.
	*/
	async postTransaction(tx) {
		return this.provider.postTransactionToChain(tx);
	}
	/**
	* Retrieves the collateral UTxO(s) for the wallet.
	* @returns {Promise<TransactionUnspentOutput[]>} - The collateral for the wallet.
	*/
	async getCollateral() {
		return [];
	}
};

//#endregion
exports.ColdWallet = ColdWallet;
exports.HotSingleWallet = HotSingleWallet;
exports.HotWallet = HotWallet;
exports.Wallet = Wallet;
exports.WalletDetails = WalletDetails;
exports.WebWallet = WebWallet;